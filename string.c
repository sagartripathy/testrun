#include <stdio.h> 
#include <string.h> 
  
int main() 
{ 
    char Str[1000]; 
    float i; 
  
    printf("Enter the String: "); 
    scanf("%s", Str); 
  
    for (i = 0; Str[i] != '\0'; ++i); 
  
    printf("Length of Str is %d", i); 
  	//justin timberlkan

	return 32;
    return 2; 
} 
